plugins {
    id 'java'
    id 'org.jetbrains.kotlin.jvm'  version '1.9.23'
}

repositories {
    mavenLocal()
    mavenCentral()
    maven {
        name = "GitHubPackages"
        url = uri("https://maven.pkg.github.com/grupo6-ingsis/printscript2")
        credentials {
            username = project.findProperty("gpr.user") ?: System.getenv("USERNAME")
            password = project.findProperty("gpr.key") ?: System.getenv("TOKEN")
        }
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

test {
    minHeapSize = "5m" // initial heap size
    maxHeapSize = "7m" // maximum heap size
}

dependencies {
    testImplementation 'junit:junit:4.13.1'
    implementation 'org.gudelker:ast:1.0.7'
    implementation 'org.gudelker:lexer:1.0.7'
    implementation 'org.gudelker:parser:1.0.7'
    implementation 'org.gudelker:interpreter:1.0.7'
    implementation 'org.gudelker:formatter:1.0.7'
    implementation 'org.gudelker:analyzer:1.0.7'
    implementation 'org.gudelker:token:1.0.7'
    implementation 'org.gudelker:utilities:1.0.7'
    implementation 'org.gudelker:cli:1.0.7'
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8"
}

test {
    environment "BEST_FOOTBALL_CLUB", "San Lorenzo"
    useJUnit()
}
